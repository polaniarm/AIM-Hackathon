# Server-only configuration. Never expose these with NEXT_PUBLIC_.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5-mini
ASK_ME_LLM_MODE=mock
# Minimum 16 characters; use a long random value for any deployed demo.
ASK_ME_ADMIN_SECRET=replace-with-a-long-random-secret
